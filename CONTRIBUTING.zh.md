> **语言 (Language)**: [English](CONTRIBUTING.md) | 简体中文

# 为 Claude Code 最佳实践做贡献

感谢您对本项目的关注!本指南将帮助您开始贡献。

## 目录

- [行为准则](#行为准则)
- [如何贡献?](#如何贡献)
- [开发环境设置](#开发环境设置)
- [项目结构](#项目结构)
- [编码标准](#编码标准)
- [提交信息指南](#提交信息指南)
- [拉取请求流程](#拉取请求流程)
- [文档指南](#文档指南)
- [测试要求](#测试要求)

---

## 行为准则

### 我们的承诺

我们致力于为所有贡献者提供友好和包容的环境,无论经验水平、性别、性别认同和表达、性取向、残疾、外表、体型、种族、民族、年龄、宗教或国籍如何。

### 我们的标准

**积极的行为包括:**

- 使用友好和包容的语言
- 尊重不同的观点和经历
- 优雅地接受建设性批评
- 专注于对社区最有利的事情
- 对其他社区成员表现出同理心

**不可接受的行为包括:**

- 恶意攻击、侮辱性/贬损性评论和人身攻击
- 公开或私下骚扰
- 未经许可发布他人的私人信息
- 其他可合理认为不适当的行为

### 执行

不可接受的行为可以向项目维护者报告。所有投诉将得到及时和公平的审查和调查。

---

## 如何贡献?

### 报告错误

**提交错误报告之前:**

- 检查[现有问题](https://github.com/ka1rax568/Claude-Code-Best-Practices/issues)以避免重复
- 收集相关信息(操作系统、Node.js 版本、Claude Code 版本、错误消息)

**提交错误报告时,请包括:**

- 清晰、描述性的标题
- 重现问题的步骤
- 预期行为与实际行为
- 截图或错误日志(如适用)
- 您的环境详细信息

### 建议增强功能

**欢迎对以下方面提出增强建议:**

- 新的自定义命令或代理
- 文档改进
- 新的示例或用例
- 工具优化

**建议增强功能时:**

- 使用清晰、描述性的标题
- 提供所提议功能的详细描述
- 解释为什么这个增强功能会有用
- 包括示例或模拟图(如适用)

### 贡献代码

**适合初学者的问题:**

- 文档拼写错误或说明
- 添加代码注释
- 编写额外的测试用例
- 改进错误消息

**更复杂的贡献:**

- 新的自定义命令
- 额外的示例
- 新的文档部分
- 代理改进

---

## 开发环境设置

### 前置要求

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0
- **Git**
- **Claude Code CLI** ([安装指南](https://github.com/anthropics/claude-code))

### 初始设置

```bash
# 1. 在 GitHub 上 Fork 仓库(点击 "Fork" 按钮)

# 2. 克隆您的 fork
git clone https://github.com/YOUR_USERNAME/Claude-Code-Best-Practices.git
cd Claude-Code-Best-Practices

# 3. 添加上游远程仓库
git remote add upstream https://github.com/anthropics/Claude-Code-Best-Practices.git

# 4. 安装根目录依赖
npm install

# 5. 安装 TDD 演示依赖
cd examples/tdd-demo
npm install
cd ../..

# 6. 运行测试以验证设置
npm test

# 7. 运行 TDD 示例测试
cd examples/tdd-demo && npm test && cd ../..
```

### 开发工作流

#### 1. 创建功能分支

```bash
# 更新您的主分支
git checkout main
git pull upstream main

# 创建功能分支
git checkout -b feat/your-feature-name

# 或者用于错误修复
git checkout -b fix/bug-description
```

#### 2. 进行更改

遵循我们的[编码标准](#编码标准)并:

- 首先编写测试(TDD 方法)
- 保持更改专注和原子化
- 根据需要更新文档
- 如果引入新功能,添加示例

#### 3. 测试您的更改

```bash
# 运行代码检查器
npm run lint

# 运行格式检查器
npm run format:check

# 运行所有测试
npm test

# 运行 TDD 示例测试
cd examples/tdd-demo && npm run test:coverage && cd ../..
```

#### 4. 提交您的更改

遵循[约定式提交](#提交信息指南):

```bash
# 暂存您的更改
git add .

# 使用约定式消息提交
git commit -m "feat(scope): add new feature"

# 或使用我们的自定义命令
# (如果您已将 .claude/ 复制到此仓库)
claude /conventional-commit
```

#### 5. 推送并创建 PR

```bash
# 推送到您的 fork
git push origin feat/your-feature-name

# 前往 GitHub 并创建拉取请求
# 从您的分支到 upstream/main
```

### 运行特定测试

```bash
# 运行特定测试文件
npm test -- path/to/test.spec.js

# 在监视模式下运行测试
npm run test:watch

# 使用覆盖率运行
npm run test:coverage

# 仅运行 TDD 示例测试
cd examples/tdd-demo
npm test
npm run test:watch
npm run test:coverage
```

### 本地开发技巧

**在此项目中使用 Claude Code:**

```bash
# .claude/ 目录包含此项目的配置
# 您可以在本地测试对命令/代理的更改:

# 1. 对 .claude/commands/your-command.sh 进行更改
# 2. 确保它是可执行的: chmod +x .claude/commands/your-command.sh
# 3. 测试它: claude /your-command

# 对于代理:
# 1. 编辑 .claude/agents/your-agent/AGENT.md
# 2. 通过提示 Claude Code 使用代理来测试
```

**测试文档更改:**

```bash
# 检查 Markdown 格式
npm run lint:markdown

# 检查断开的链接(需要互联网)
npm run check:links

# 拼写检查
npm run spell:check
```

### 保持您的 Fork 更新

```bash
# 获取上游更改
git fetch upstream

# 更新您的主分支
git checkout main
git merge upstream/main

# 推送更新到您的 fork
git push origin main

# 变基您的功能分支(如果需要)
git checkout feat/your-feature
git rebase main
```

### 故障排除

**设置后测试失败:**

```bash
# 清除 node_modules 并重新安装
rm -rf node_modules package-lock.json
npm install

# 对于 TDD 示例
cd examples/tdd-demo
rm -rf node_modules package-lock.json
npm install
cd ../..
```

**Claude Code 无法识别更改:**

```bash
# 重启 Claude Code CLI
# CLI 启动时加载 .claude/ 文件的更改
```

**命令权限问题:**

```bash
# 确保命令是可执行的
chmod +x .claude/commands/*.sh
```

---

## 项目结构

```
.
├── .claude/                # Claude Code 配置
│   ├── CLAUDE.md          # 主配置文件
│   ├── settings.json      # 安全和工具设置
│   ├── commands/          # 自定义斜杠命令
│   └── agents/            # 专门的子代理
├── docs/                  # 文档文件
├── examples/              # 实用示例
│   └── tdd-demo/         # TDD 演示项目
├── templates/             # 可重用模板
└── tests/                 # 项目级测试
```

**关键文件:**

- `.claude/CLAUDE.md`: Claude Code 的项目上下文
- `PLAN.md`: 开发路线图
- `package.json`: Node.js 项目配置

---

## 编码标准

### JavaScript/Node.js 风格指南

我们遵循由 ESLint 和 Prettier 强制执行的严格风格指南:

**命名约定:**

- `camelCase` 用于变量和函数
- `PascalCase` 用于类和构造函数
- `UPPER_SNAKE_CASE` 用于常量
- 描述性名称(除循环计数器外避免单字母)

**代码风格:**

- 2 个空格缩进(无制表符)
- 需要分号
- 字符串使用单引号(除非避免转义)
- 多行对象/数组中的尾随逗号
- 最大行长度: 100 个字符

**文件组织:**

- 每个文件一个类/模块
- 分组导入: 内置 → 外部 → 内部
- 在文件底部导出

### 运行代码检查器

```bash
# 检查风格问题
npm run lint

# 自动修复问题
npm run lint:fix

# 检查格式
npm run format:check

# 自动格式化文件
npm run format
```

### Shell 脚本

对于自定义命令(`.claude/commands/*.sh`):

- 使用 `#!/bin/bash` shebang
- 包含描述性注释
- 使用有意义的变量名
- 优雅地处理错误
- 使用适当的状态码退出

---

## 提交信息指南

我们遵循[约定式提交](https://www.conventionalcommits.org/)规范:

### 格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型

- `feat`: 新功能
- `fix`: 错误修复
- `docs`: 文档更改
- `style`: 代码风格更改(格式化,无逻辑更改)
- `refactor`: 代码重构
- `test`: 添加或更新测试
- `chore`: 维护任务(依赖项,构建配置)

### 示例

```
feat(commands): add /review command for automated code review

Implements comprehensive code review functionality including:
- Security vulnerability detection
- Performance analysis
- Best practice checks

Closes #42
```

```
fix(agent): handle empty file lists in code-reviewer

Previously crashed when no files were provided.
Now returns early with helpful message.

Fixes #58
```

```
docs(security): expand deny list configuration examples

Added examples for:
- Multi-environment setups
- Custom credential patterns
- Large monorepo scenarios
```

### 提交信息规则

- 使用祈使语气("add feature"而不是"added feature")
- 第一行 ≤ 72 个字符
- 用空行分隔主题和正文
- 在页脚引用问题/PR
- 解释_是什么_和_为什么_,而不是_如何_

---

## 拉取请求流程

### 提交之前

1. **创建功能分支**

   ```bash
   git checkout -b feat/my-new-feature
   ```

2. **进行更改**
   - 遵循编码标准
   - 为新功能添加测试
   - 更新文档

3. **运行所有检查**

   ```bash
   npm run lint
   npm run format:check
   npm test
   ```

4. **提交您的更改**

   ```bash
   git add .
   git commit -m "feat(scope): description"
   ```

5. **推送到您的 fork**
   ```bash
   git push origin feat/my-new-feature
   ```

### 创建拉取请求

1. 访问[原始仓库](https://github.com/ka1rax568/Claude-Code-Best-Practices)
2. 点击 "New Pull Request"
3. 选择您的 fork 和分支
4. 填写 PR 模板:

```markdown
## 描述

更改的简要描述

## 更改类型

- [ ] 错误修复
- [ ] 新功能
- [ ] 文档更新
- [ ] 重构

## 测试

- [ ] 所有现有测试通过
- [ ] 为新功能添加了新测试
- [ ] 手动测试完成

## 检查清单

- [ ] 代码遵循项目风格指南
- [ ] 自我审查完成
- [ ] 为复杂逻辑添加了注释
- [ ] 文档已更新
- [ ] 无破坏性更改(或已记录)

## 相关问题

Closes #123
```

### 审查过程

1. **自动检查**: CI 将运行测试和代码检查器
2. **维护者审查**: 维护者将审查您的代码
3. **处理反馈**: 进行请求的更改
4. **批准**: 一旦批准,维护者将合并

### 合并后

- 删除您的功能分支
- 更新您的 fork 的主分支
- 庆祝! 🎉

---

## 文档指南

### 写作风格

- **清晰简洁**: 使用简单的语言
- **主动语态**: "运行命令"而不是"命令应该被运行"
- **示例**: 为复杂概念包含代码示例
- **结构**: 使用标题、列表和代码块

### 文档类型

**概念性文档**(`docs/*.md`):

- 解释_为什么_和_何时_使用功能
- 如有帮助,包含图表
- 链接到相关文档

**参考文档**:

- 描述每个选项_做什么_
- 列出所有参数
- 为每个选项包含示例

**教程**(`examples/*/README.md`):

- 分步说明
- 解释每个步骤
- 显示预期输出

### 代码示例

- **可运行**: 所有示例应在复制粘贴后工作
- **注释**: 解释不明显的代码
- **完整**: 包括导入和设置
- **测试**: 验证示例是否有效

### Markdown 格式

```markdown
# 顶级标题(每个文档一个)

## 章节标题

### 小节标题

**粗体**用于强调,_斜体_用于术语

`内联代码`用于命令、文件名、变量

\`\`\`bash

# 指定语言的代码块

npm install
\`\`\`

> 引用重要注意事项

- 项目符号列表
- 用于项目

1. 编号列表
2. 用于顺序步骤

[链接文本](URL)
```

---

## 测试要求

### 测试覆盖率

- **新功能**: 必须包含测试
- **错误修复**: 添加回归测试
- **最小覆盖率**: 80%(由 CI 强制执行)

### 运行测试

```bash
# 运行所有测试
npm test

# 开发的监视模式
npm run test:watch

# 覆盖率报告
npm run test:coverage
```

### 编写测试

**结构:**

```javascript
describe("FeatureName", () => {
  describe("methodName", () => {
    it("should do something specific", () => {
      // Arrange: 设置测试数据
      const input = "test";

      // Act: 执行代码
      const result = myFunction(input);

      // Assert: 验证结果
      expect(result).toBe("expected");
    });
  });
});
```

**最佳实践:**

- 每个测试一个断言(如果可能)
- 描述性测试名称
- 测试边缘情况和错误条件
- 使用有意义的测试数据
- 避免测试相互依赖

---

## 有问题?

- **GitHub 讨论**: [提问](https://github.com/ka1rax568/Claude-Code-Best-Practices/discussions)
- **问题**: [报告错误或建议功能](https://github.com/ka1rax568/Claude-Code-Best-Practices/issues)

---

**感谢您为 Claude Code 最佳实践做出贡献!** 🙏

您的贡献帮助世界各地的开发人员使用 AI 辅助构建更好的软件。
